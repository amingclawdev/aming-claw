"""Packaged dashboard static assets.

`npm --prefix frontend/dashboard run build` refreshes this package directory
before building a wheel. The governance server falls back to these files when
the repo-local frontend/dashboard/dist directory is not present.
"""
